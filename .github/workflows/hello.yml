# .github/workflows/hello.yml
name: Hello GitHub Actions

on:
  push:
    branches: [ main ]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Say Hello
      run: echo "🎉 GitHub Actions 시작을 환영합니다!"
